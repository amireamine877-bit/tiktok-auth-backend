// placeholder: connect to a DB (Postgres / Mongo / Supabase) if needed
// Example (pg) or mongoose can be used; for MVP you can use Supabase instead.
module.exports = {
  // add connection helpers here if you choose Postgres or Mongo
};
