// Placeholder - implement report assembly or call to MAKE to build PDF
async function generateReportForAccount(accountId, range) {
  // call MAKE or generate locally
  return { accountId, reportUrl: null };
}

module.exports = { generateReportForAccount };
